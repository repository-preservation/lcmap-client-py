base_context = "/api"
auth_context = base_context + "/auth"
compatibility_context = base_context + "/compatibility"
data_context = base_context + "/data"
jobs_context = base_context + "/jobs"
models_context = base_context + "/models"
notifications_context = base_context + "/notifications"
system_context = base_context + "/system"
users_context = base_context + "/users"
